<ol class="has-flow" reversed>
  {% for post in collections.post | reverse %}
    {%- set date = post.date | readableDate -%}
    <li>
      <article id="{{ date | slug }}" class="has-flow">
        <header>
          <h3><a href="#{{ date | slug }}">{{ date }}</a></h3>
        </header>
        <div class="has-flow">
          {{ post.templateContent | safe }}
        </div>
      </article>
    </li>
  {% endfor %}
</ol>
