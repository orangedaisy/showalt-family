<!DOCTYPE html>
<html lang="en"{% if 'recipe' in page.url %} class="recipe"{% endif %}>

{% include "head.njk" %}

<body>
  {% include "header.njk" %}

  <main>{{ content | safe }}</main>

  <script>
    if (window.netlifyIdentity) {
      window.netlifyIdentity.on("init", user => {
        if (!user) {
          window.netlifyIdentity.on("login", () => {
            document.location.href = "/admin/";
          });
        }
      });
    }
  </script>
</body>
</html>
