<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>
    {% if title %}
      {{ title }} | 
    {% endif %}
    Showalter Recipes
  </title>
</head>
<body>
  <h1>{{ title }}</h1>

  <h2>Tags</h2>
  <ul>
  {% for tag in tags %}
    <li>{{ tag }}</li>
  {% endfor %}
  </ul>
  <hr>

  <h2>Ingredients</h2>
  <ul>
  {% for ingredient in ingredients %}
    <li>{{ ingredient }}</li>
  {% endfor %}
  </ul>
  <hr>

  <h2>Method</h2>
  <div>{{ method | safe }}</div>
  <hr>

  <h2>Source</h2>
  <div>{{ source | safe }}</div>
</body>
</html>
